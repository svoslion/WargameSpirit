{% set article_show = {{ path('article_show', { 'slug': article.slug }) }} %}

<article class="mb-5">
	<div class="row">
		<div class="col-md-5">
           <a href="{{ subject_show }}">
              <img src="assets/img/epingle-en-papier.png" alt="" loading="lazy" width="100" height="50">
           </a>
        </div>
	</div>
</article>
